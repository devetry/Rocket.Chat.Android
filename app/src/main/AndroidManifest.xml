<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ytpconnect.rocket.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="ytpconnect.rocket.android.app.RocketChatApplication"
        tools:replace="android:name"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity
            android:name="ytpconnect.rocket.android.authentication.ui.AuthenticationActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:theme="@style/AuthenticationTheme"
            android:windowSoftInputMode="adjustResize">
            <!--YTP Entry point-->
            <intent-filter>
                <action android:name="com.devetry.chat.START"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <!--TODO: Remove this regular entry point-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="auth"
                    android:scheme="rocketchat" />
                <data
                    android:host="go.rocket.chat"
                    android:path="/auth"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <activity
            android:name="ytpconnect.rocket.android.server.ui.ChangeServerActivity"
            android:theme="@style/AuthenticationTheme" />

        <activity
            android:name="ytpconnect.rocket.android.main.ui.MainActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <activity
            android:name="ytpconnect.rocket.android.webview.ui.WebViewActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <activity
            android:name="ytpconnect.rocket.android.webview.sso.ui.SsoWebViewActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <activity
            android:name="ytpconnect.rocket.android.webview.oauth.ui.OauthWebViewActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <activity
            android:name="ytpconnect.rocket.android.chatroom.ui.ChatRoomActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <activity
            android:name="ytpconnect.rocket.android.chatinformation.ui.MessageInfoActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden" />

        <!-- TODO: Change to fragment-->
        <activity
            android:name="ytpconnect.rocket.android.settings.password.ui.PasswordActivity"
            android:theme="@style/AppTheme" />

        <receiver
            android:name="ytpconnect.rocket.android.push.DirectReplyReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="chat.rocket.android.ACTION_REPLY" />
            </intent-filter>
        </receiver>

        <service
            android:name="ytpconnect.rocket.android.chatroom.service.MessageService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="12ac6e94f850aaffcdff52001af77ca415d06a43" />
    </application>

</manifest>
